#very simple password validator for register or password change forms
password strength can be set with TYPE parametr 
jquery is required for this example, but if needed it can be replaced with pure js

